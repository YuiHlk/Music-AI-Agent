---
tags: [project/music-ai-agent, topic/architecture]
status: active
---

# 架构与数据流

导航：[[00-首页]] · [[PROJECT_STRUCTURE]] · [[AGENTS]]

## 逻辑分层

```mermaid
%%{init: {"flowchart": {"useMaxWidth": true, "htmlLabels": false}}}%%
flowchart TB
    subgraph Entry["入口适配器"]
        UI["Vue 工作台"]
        REST["REST / SSE"]
        CLIENT["外部 AI 客户端"]
        MCP["MCP Server"]
        UI --> REST
        CLIENT --> MCP
    end

    subgraph Intelligence["意图理解与工具编排"]
        AGENT["内部 MusicCreatorAgent"]
        TOOLS["内部 Java Tools"]
        PARSER["RequirementParser：LLM / Rule"]
        AGENT --> TOOLS
    end

    subgraph Core["应用与领域核心"]
        APP["Application：用例编排"]
        DOMAIN["Domain：音乐规则"]
        PORT["Ports：对外端口"]
        APP --> DOMAIN
        APP --> PORT
    end

    subgraph Output["出口适配器"]
        direction LR
        EXPORT["Export：MIDI / MusicXML"]
        INFRA["Infrastructure：MyBatis / OS"]
    end

    REST -->|"普通生成"| APP
    REST -->|"chat"| AGENT
    TOOLS --> APP
    APP -->|"自然语言解析"| PARSER
    MCP -->|"工具调用"| APP
    APP --> EXPORT
    PORT --> INFRA
```

箭头表示实际运行时调用方向。内部 Agent 通过 `MusicCreationTools` 直接调用 Application，不通过 MCP 绕回本进程；MCP 专门服务项目外部的 Agent。Application 只依赖端口，不直接依赖 MyBatis、MySQL 或操作系统能力。

详细边界见 [[05-模型、内部Agent与MCP边界]]。

## 生成链路

```mermaid
%%{init: {"flowchart": {"useMaxWidth": true, "htmlLabels": false}}}%%
flowchart TB
    subgraph Request["① 受理请求"]
        direction LR
        PROMPT["REST / MCP自然语言"] --> PARSE["RequirementParser"]
        STRUCTURED["MCP结构化约束"] --> CONSTRAINTS["CreationConstraints"]
        PARSE --> CONSTRAINTS
        CONSTRAINTS --> TASK["创建异步任务"]
        TASK --> ACCEPT["返回 202 与任务 ID"]
        ACCEPT --> SUBSCRIBE["订阅 SSE"]
    end

    subgraph Generate["② 后台生成"]
        direction LR
        SCORE["生成 Score"]
        SCORE --> VALIDATE["时值与指法校验"]
        VALIDATE --> SAVE["保存版本和导出物"]
    end

    subgraph Result["③ 返回结果"]
        direction LR
        EVENT["发布完成事件"] --> PUSH["SSE 推送"]
        PUSH --> PREVIEW["展示预览和导出入口"]
    end

    TASK --> SCORE
    SAVE --> EVENT
```

自然语言路径先由服务器 `RequirementParser` 解析；结构化MCP路径直接获得约束，不调用服务器模型。两条路径都会先持久化任务，再由后台线程生成、校验、保存和导出。

## 核心边界

- Domain 保持纯 Java，不依赖 Spring、HTTP、MyBatis 或 LangChain4j。
- Agent 理解意图，但不直接写数据库或最终 MusicXML。
- 服务器模型通过通用 `llm` Profile 配置，不与 DeepSeek 名称绑定。
- MCP 的结构化约束工具允许外部模型跳过服务器 RequirementParser，避免消耗服务器模型 Key。
- `Score` 是生成、修改、校验和导出的唯一事实来源。
- Application 调用端口，Infrastructure 实现端口。
- 当前仍是单轨规则生成；多轨、和声与结构化 SongPlan 是下一阶段。
